## Initial Prompt
Press Space while an image is focused to enlarge it like macOS Quick Look, including equivalent functionality on Omarchy.

## Plan
Add a failing full-window shortcut test; route Space from selected images to the existing preview; support Space/Escape dismissal and focus restoration; preserve panning and suppress key repeat; update shortcut help and test Cocoa/Wayland.

## Next Steps
Open a new window on Omarchy to use the updated binary. Existing processes remain running to preserve unsaved work.

## Implementation Summary
- Space opens the selected image in the existing enlarged, fitted preview window on both platforms.
- Space and Escape close the preview and return focus to the source canvas. Repeated key events do not repeatedly toggle the preview.
- Space-drag panning remains available when an image is not selected. Previewing does not change the document or its canvas viewport.
- Updated native macOS and shared keyboard shortcut tables and README.
- Observed the new full-window test fail before implementation, then pass. Native Cocoa UI tests passed for Space open/close/Escape and existing image drop/resize/preview. Full canvas and macwindow suites passed.
- Omarchy Qt 6.11.2: the focused canvas regression and real Wayland full-window Space preview test passed.
- Rebuilt/restarted macOS gracefully with recovery and atomically updated the standard Omarchy binary. No installer or commit created.
