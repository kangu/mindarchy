# Omarchy 0.1.4 package

## Initial Prompt
Build Omarchy version 0.1.4 on the Linux build machine and copy the installer here for a future GitHub release. Do not record host addresses or similar details in committed files.

## Plan
1. Sync packaging sources to the existing remote checkout.
2. Run `VERSION=0.1.4 bash scripts/build_omarchy.sh`.
3. Copy `mindarchy-0.1.4-1-x86_64.pkg.tar.zst` into local `artifacts/omarchy/` (gitignored).

## Next Steps
Stage macOS and Windows 0.1.4 installers, then `python3 scripts/release-github.py --version 0.1.4`.

## Implementation Summary
Linux qmake failed until `src/appfont.h` included `QStringList` for `QFontDatabase::families()`. After that, makepkg produced `mindarchy-0.1.4-1-x86_64.pkg.tar.zst` (about 8.7 MB). The file is in `artifacts/omarchy/` for the GitHub publisher. No `.sig` was present. Remote host identifiers were not written into this note.
