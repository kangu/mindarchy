# Omarchy theme

## Initial Prompt
Add a dark theme as the second entry, using JetBrains Mono and a deeper connection to Omarchy's visual identity.

## Plan
Reference Omarchy's Tokyo Night palette, bundle the licensed font, add coordinated node/task styling, verify ordering and contrast, then rebuild and inspect macOS rendering.

## Next Steps
Reopen the rebuilt macOS app. The existing app-control identity mismatch prevents automatic restart. Omarchy/Windows runtime deployment was not performed.

## Implementation Summary
Added Omarchy immediately after Porcelain. Dark charcoal-blue canvas, terminal green root and completion indicators, blue/violet/amber/pink/cyan branches, pale lavender ink and restrained corners create a terminal-inspired appearance. Bundled upright and italic JetBrains Mono with SIL license and source hashes. Added example document and documentation. Existing live Omarchy shell theming stays independent of the saved map palette.

macOS build passed. Font registration, second-position ordering, theme persistence and contrast tests passed, as did the theme-picker UI test. Visually inspected the exported map and actual macOS theme picker screenshot. No installer built.
