cmake_minimum_required(VERSION 3.21)
set(MINDMAP_VERSION "0.1.0" CACHE STRING "Application release version")
project(MindmapLab VERSION ${MINDMAP_VERSION} LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_AUTOMOC ON)
set(CMAKE_AUTORCC ON)
find_package(Qt6 6.6 REQUIRED COMPONENTS Core Gui Qml Quick QuickControls2 Test)
add_library(mindmap_engine src/engine.cpp src/engine.h src/theme.cpp src/theme.h)
target_link_libraries(mindmap_engine PUBLIC Qt6::Core Qt6::Gui)
target_include_directories(mindmap_engine PUBLIC src)
add_library(mindmap_canvas src/canvas.cpp src/canvas.h)
target_link_libraries(mindmap_canvas PUBLIC mindmap_engine Qt6::Quick)
qt_add_executable(mindmap-lab src/main.cpp src/windowplacement.cpp src/windowplacement.h resources.qrc)
target_link_libraries(mindmap-lab PRIVATE mindmap_canvas Qt6::Qml Qt6::QuickControls2)
target_compile_definitions(mindmap-lab PRIVATE MINDMAP_VERSION="${PROJECT_VERSION}")
if(APPLE)
    enable_language(OBJCXX)
    target_sources(mindmap-lab PRIVATE src/macwindow.mm)
    target_link_libraries(mindmap-lab PRIVATE "-framework AppKit")
    set(app_icon "${CMAKE_CURRENT_SOURCE_DIR}/assets/icons/mindmap-blue.icns")
    target_sources(mindmap-lab PRIVATE "${app_icon}")
    set_source_files_properties("${app_icon}" PROPERTIES MACOSX_PACKAGE_LOCATION Resources)
    set_target_properties(mindmap-lab PROPERTIES
        MACOSX_BUNDLE TRUE
        MACOSX_BUNDLE_ICON_FILE mindmap-blue.icns
        MACOSX_BUNDLE_GUI_IDENTIFIER blue.mindmap.lab
        MACOSX_BUNDLE_BUNDLE_NAME "Mindmap Lab"
        MACOSX_BUNDLE_BUNDLE_VERSION "${PROJECT_VERSION}"
        MACOSX_BUNDLE_SHORT_VERSION_STRING "${PROJECT_VERSION}")
elseif(WIN32)
    enable_language(RC)
    set(APPLICATION_ICON_WINDOWS "${CMAKE_CURRENT_SOURCE_DIR}/assets/icons/mindmap-blue.ico")
    configure_file(packaging/windows-icon.rc.in windows-icon.rc @ONLY)
    target_sources(mindmap-lab PRIVATE "${CMAKE_CURRENT_BINARY_DIR}/windows-icon.rc")
    set_target_properties(mindmap-lab PROPERTIES WIN32_EXECUTABLE TRUE)
else()
    include(GNUInstallDirs)
    install(TARGETS mindmap-lab RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
    install(FILES packaging/blue.mindmap.lab.desktop DESTINATION ${CMAKE_INSTALL_DATADIR}/applications)
    foreach(size 16 24 32 48 64 128 256 512 1024)
        install(FILES assets/icons/mindmap-blue-${size}.png
            DESTINATION ${CMAKE_INSTALL_DATADIR}/icons/hicolor/${size}x${size}/apps
            RENAME blue.mindmap.lab.png)
    endforeach()
endif()
enable_testing()
if(APPLE)
    add_executable(macwindow_test tests/macwindow_test.mm src/macwindow.mm)
    target_link_libraries(macwindow_test PRIVATE Qt6::Gui "-framework AppKit")
    add_test(NAME macwindow COMMAND macwindow_test)
    set_tests_properties(macwindow PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=cocoa")
endif()
add_executable(engine_test tests/engine_test.cpp)
target_link_libraries(engine_test PRIVATE mindmap_engine Qt6::Test)
add_test(NAME engine COMMAND engine_test)
add_executable(canvas_test tests/canvas_test.cpp)
target_link_libraries(canvas_test PRIVATE mindmap_canvas Qt6::Test)
add_test(NAME canvas COMMAND canvas_test)
set_tests_properties(engine canvas PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
qt_add_executable(ui_test tests/ui_test.cpp resources.qrc)
target_link_libraries(ui_test PRIVATE mindmap_canvas Qt6::Qml Qt6::QuickControls2 Qt6::Test)
add_test(NAME ui COMMAND ui_test)
set_tests_properties(ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")

add_executable(windowplacement_test tests/windowplacement_test.cpp src/windowplacement.cpp)
target_link_libraries(windowplacement_test PRIVATE Qt6::Quick Qt6::Test)
add_test(NAME windowplacement COMMAND windowplacement_test)
set_tests_properties(windowplacement PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
