# Initial Prompt
Add image placement buttons to the right sidebar: left, right, top, and bottom.

# Plan
Add persisted image placement and shared content geometry; expose conditional inspector controls; verify editing, tasks, resizing, previews, and platform UI.

# Next Steps
Use Node → Image Placement on an image-bearing node. Existing Omarchy windows use the previous binary until reopened.

# Implementation Summary
Added four themed image placement buttons to the Node inspector, visible for a single selected node with an image. Images and content now share placement geometry across canvas rendering, inline editing, task hit targets, calendar controls, and exported previews. Placement persists in JSON, supports undo/redo, defaults to left for older files, and survives replacement. Updated README.

Validation: observed the initial persistence test fail before implementation. New model, geometry, and actual sidebar interaction tests pass. All seven macOS CTest suites passed (70.89 seconds), including the full UI suite. Cocoa sidebar and Space preview tests passed. Omarchy's real Wayland sidebar test passed. Inspected a top-placement PNG export. Rebuilt and restarted macOS; atomically updated the Omarchy binary without interrupting existing windows. No installer built.
